#include <stdio.h>
#include <stdlib.h>
/**
 * main - entry point
 * Description: prints alphabets in lowercase
 * Return; returns 0
 */

int main(void)
{
	int al;

	for (ch = 'a', ch <= 'z', ch++)
	{
		puchar(ch);
	}
	putchar('\n');
	return (0);
}
