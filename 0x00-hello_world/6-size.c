#include <stdio.h>
/**
 * main - print size of data types
 * Description: use printf and sizeof
 * Return: 0
 */
int main()
{
	printf("%zu, %zu, %zu, %zu, %zu", sizeof(int),sizeof(char),sizeof(long int),sizeof(long long int)sizeof(float));
	return (0);
}
